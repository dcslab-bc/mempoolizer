import React, { useRef, useEffect} from "react";
import * as d3 from "d3";

const MempoolView = (props) => {
  // const mempoolViewSvg = useRef(null);

  useEffect(() => {
    // Nadine's Code
  });

  return (
    <div style={{width: 1400, height: 450, border: "1px solid #999", boxSizing: "border-box"}}>
      MempoolView Area
		</div>
  )
};
export default MempoolView;